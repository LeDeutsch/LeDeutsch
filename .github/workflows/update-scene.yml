name: Update Guild Scene

on:
  schedule:
    - cron: '*/30 * * * *'
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: update-scene
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install Pillow

      - name: Generate scene
        env:
          GITHUB_USER: LeDeutsch
          # PROFILE_PAT (PAT with read:user scope) enables commit counting across
          # all repos (public + private). Falls back to the default GITHUB_TOKEN
          # if the secret is missing (public repos only).
          GITHUB_TOKEN: ${{ secrets.PROFILE_PAT || secrets.GITHUB_TOKEN }}
        run: python scripts/generate_scene.py

      - name: Commit if changed
        run: |
          git config user.name "guild-bot"
          git config user.email "guild-bot@users.noreply.github.com"
          git add output/ README.md
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore(scene): refresh guild hall [skip ci]"
            git push
          fi
